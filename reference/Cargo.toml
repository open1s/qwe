[package]
name = "pwe-reference"
version = "0.2.0"
edition = "2021"
rust-version = "1.81"
publish = false
license = "Apache-2.0"

[dependencies]
pwe-api = { path = ".." }
# Language front end grammar (PEST). Pinned below 2.8 for MSRV compatibility
# with the crate's rust-version (1.81).
pest = ">=2.9.1"
pest_derive = ">=2.9.1"
